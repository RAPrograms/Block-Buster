name: tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: "Test"
    runs-on: ubuntu-latest
    steps:
      - name: "Vitests"
        run: |
          npm ci
          npm run test